# Docker Environment Configuration
# Copy this file to .env and customize for your environment

# Display for X11 forwarding (Linux/Mac GUI support)
# For Linux: usually :0 or :1
# For Mac with XQuartz: host.docker.internal:0
# For Windows: not applicable (use headless mode)
DISPLAY=:0

# Custom dataset path on host machine
# Mount your dataset directory here
DATASET_PATH=./datasets

# GPU device selection (optional)
# Examples:
#   all - use all GPUs
#   0 - use only GPU 0
#   0,1,2,3 - use specific GPUs
NVIDIA_VISIBLE_DEVICES=all

# Container timezone (optional)
TZ=Asia/Seoul
